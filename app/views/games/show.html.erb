<%= render :partial => 'resources/show', :locals  => { :resource => @game } %>

<script>
	//Prevent scrolling from iframe
	try {
		$(document).on('scroll', function(e){
			if(document.activeElement.className == "gameIframe"){
				window.scrollTo(0,0);
			}
		});
	}catch(e){}
</script>