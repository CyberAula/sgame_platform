<!DOCTYPE html>
<html>
<head>
<style>
	body, html {
		width: 100%;
		height: 100%;
		overflow: hidden;
		margin: 0px !important;
	}
</style>
<meta charset="utf-8">
<script type="text/javascript">
	<% if SgamePlatform::Application.config.domain.start_with?("localhost") %>
		var protocol = "http:";
	<% else %>
		var protocol = (document.location.protocol==="file:" ? "http:" : document.location.protocol);
	<% end %>
	
	<%
		resources = ["/sgame_api/SGAME_VLE_Gateway.js"]
		resources.each do |resource| %>
		document.write(unescape("%3Cscript src='" + protocol + "//<%=SgamePlatform::Application.config.domain + resource%>' type='text/javascript' async %3E%3C/script%3E"));
	<% end %>
</script>
</head>
<body id="game_iframe">
	<iframe src="<%=Utils.checkUrlProtocol(SgamePlatform::Application.config.full_domain + game_path(game, format: "full"),((SgamePlatform::Application.config.domain).start_with?("localhost") ? "http" : "https"))%>" class="sgame_instance" width="100%" height="100%" border:0" iframeborder="0" frameborder="0" iframeelement.frameborder="0;" webkitallowfullscreen="true" allowfullscreen="true" mozallowfullscreen="true"></iframe>
</body>
</html>